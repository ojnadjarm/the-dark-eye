# The Dark Eye — body (slice A2, not started)

Electron app on Windows: the Eye overlay + voice worker (sherpa-onnx:
Parakeet STT, Kokoro TTS) + MCP server (listen / speak / ring /
agent_status / present). See `../spec/A-the-eye.md` §8.
