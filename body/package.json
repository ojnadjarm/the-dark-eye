{
  "name": "@dark-eye/body",
  "private": true,
  "version": "0.1.0",
  "description": "The Dark Eye — body: node (bridge, voice worker, eye-render) with Electron for the canvas.",
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "electron": "^42.0.0",
    "sherpa-onnx-node": "^1.13.4"
  }
}
